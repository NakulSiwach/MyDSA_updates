

5<<1 left shift
what it does is moves all 1 bits(set bits) to left, multiply by 2.
opposite with right shift.

paddings are done with 0 always.
  
xor ^ , when bits are same , it reverses , x^x = 0.
